// Distributing the Total Candies Among Students

let totalCandies = 95;
let numStudents = 8;

let candiesLeftOver = totalCandies % numStudents; //used the modulus operator because % directly returns the remainder after division.

console.log(candiesLeftOver);